'use strict';

describe('myApp.menu module', function() {

  beforeEach(module('myApp.menu'));

  describe('menu controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('MenuCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});